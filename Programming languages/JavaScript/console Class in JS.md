[Documentation](https://developer.mozilla.org/en-US/docs/Web/API/console)

We can do a lot of stuff in console, like send errors, messages, warnings, tables etc,.

For now, lets have a look at:
## console.log("Hello There");
What this does is outputs whatever is given as parameter in the web-pages console.

